services:
  k8s:
    build: .
    network_mode: host

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/workspace

    environment:
      KUBECONFIG: /workspace/.kube/config

    working_dir: /workspace
